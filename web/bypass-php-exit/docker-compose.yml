version: '2'

services:
    web:
        build: .
        ports:
            - '8080:8080'
        stdin_open: true
        tty: true
        volumes:
            - ./source/upload:/var/www/html/upload
